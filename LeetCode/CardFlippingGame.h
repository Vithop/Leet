class Solution {
public:
    int flipgame(vector<int>& fronts, vector<int>& backs) {
    	int min = INT_MAX;

        for (int i = 0; i < fronts.size(); ++i)
        {
        	count(fronts.begin(), fronts.end(), fronts[i])
        }
    }
};